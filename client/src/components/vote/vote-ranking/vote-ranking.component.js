import template from './vote-ranking.html';

const Component = {
  bindings: {
    round: '=',
    tasks: '=',
  },
  controller,
  template,
};

function controller() {
  const vm = this;
}

export default Component;
